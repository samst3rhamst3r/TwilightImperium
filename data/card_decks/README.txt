The configuration files in this folder only contain entries
where a particular card exists more than once in the deck. 
Unique cards are not mentioned, and are defaulted
to have only one instance in each deck that is generated.
