from enum import auto

from app.config.enum import GameStrEnum

class PlayerColor(GameStrEnum):
    BLACK = auto()
    BLUE = auto()
    GREEN = auto()
    RED = auto()
    PURPLE = auto()
    YELLOW = auto()
